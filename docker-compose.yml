version: '3'

services:
  server:
    build:
      dockerfile: ./docker/server/Dockerfile
    container_name: flask_latex_sample
    ports:
      - "5001:5001"
    tty: true
    volumes:
      - ../src:/root/src
